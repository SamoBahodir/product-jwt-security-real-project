server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/security
    username: postgres
    password: 1273
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      format_sql: true
jwt:
  token:
    secret: jwtjavasamo
    validity: 864000000